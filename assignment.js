const userInput = 10;
let initialValue;

initialValue = 18 + userInput;

initialValue = initialValue - 8;
initialValue = initialValue * 9;
initialValue = initialValue / 2;

/* The value stored in the user input variable didn't change*/

alert(userInput);
alert(initialValue);
